import {
    SocialBtn
} from './StyledSocialButton';

const SocialButton = ({children}) => {
    return (
        <SocialBtn>
            {children}
        </SocialBtn>
    )
}

export default SocialButton;